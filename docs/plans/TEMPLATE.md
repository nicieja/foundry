# <Title>

## Context

<!-- Why this task exists: the problem, what prompted it, and what is true when it is done. Name the roadmap milestone. -->

<!-- Under a fuzzy goal, one line: `Rung: R<n> — authorised by: docs/evidence/bet-<n>/r<n-1>/verdict.md`.
     A build task with no PASS behind it does not get a plan; it gets sent back to `prove`. Delete this line when the goal is not fuzzy. -->

## Research

<!-- What was read or measured before deciding: code paths, docs, prior art, numbers. Delete if nothing was researched. -->

## Considerations

<!-- The options weighed and the one chosen, with the reason. Delete if there was no real choice. -->

## Implementation

<!-- Ordered checkboxes. Each step is one verifiable change. Name the files. -->

- [ ]

## Open questions

<!-- Anything that needs the user. Each becomes a Blocked & escalations entry if it stops the work. -->

## Out of scope

<!-- What this task deliberately does not do, and where that work goes instead. -->

## Verification

<!-- How to prove it works: commands, expected output, behavior to run by hand. `bin/ci` green is the floor, not the ceiling.
     Under a fuzzy goal, the ceiling is the rung's bar met in its `verdict.md` — a count ruled by the arbiter, never your reading of the transcripts. -->
